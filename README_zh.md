**中文** | [English](README.md)

# AirPods Fix

一个原生 macOS 应用，专门用来诊断和修复 AirPods 音频问题。

AirPods 连上了但没声音、音质不对、麦克风不好使？这个工具帮你定位问题并修复，不用重启 Mac。

推荐路径：直接从 GitHub Releases 下载打包好的 app 并运行。
更详细的运行时、打包和源码构建说明放在 [RUNTIME.md](RUNTIME.md)。

## 功能

### 设备检测与电量

- 自动扫描已连接的 AirPods（通过蓝牙）
- 同时连了多副 AirPods 时，可手动选择要诊断和修复的目标设备
- 实时显示左耳、右耳、充电盒的电量
- 颜色状态指示（绿/黄/红）

### 音频诊断

- 检查 AirPods 是否为当前默认输出设备
- 检测音频通道配置（立体声 / 单声道）
- 读取采样率（24kHz、44.1kHz、48kHz）并识别当前模式：
  - **立体声模式**（44.1kHz/48kHz）：完整音质
  - **单声道 24kHz**：音质降低，通常是路由问题
  - **通话模式**（8kHz/16kHz）：麦克风激活，扬声器音质下降
- 监控系统音量和静音状态
- 异常标记：输出设备不对、单声道/通话模式、已静音、音量极低

### 扬声器测试

播放系统测试音（Ping + Glass），确认两只耳机都能正常出声。

### 麦克风测试

实时麦克风电平监测：

- 基于 RMS 的电平条，带平滑动画
- 信号强度指示：无信号 / 微弱 / 正常 / 较响 / 很响
- 峰值标记，自动衰减
- 渐变色彩条（绿 -> 黄 -> 橙 -> 红）
- 静音自动重试（处理权限授予延迟）

### 音频修复（3 级）

**1. 软修复** - 刷新音频路由（不中断服务）

- 如果静音则取消静音
- 如果音量低于 10% 则调高
- 先切换到内置扬声器，再切回 AirPods
- 强制 macOS 重建音频路由，不动核心服务

**2. 中修复** - 重启 `coreaudiod`

- 终止并重启 macOS 核心音频守护进程
- 短暂音频中断（约 3 秒）
- 恢复后重新诊断音频状态

**3. 硬修复** - 蓝牙重连

- 在打包发布版中使用内置 `blueutil`，本地构建则使用系统里的 `blueutil`
- 等待蓝牙握手完全关闭
- 重新连接并重建音频通道
- 连接后运行诊断验证结果

每级修复都有实时进度条和步骤描述。
应用通过一个**一键修复**入口依次尝试这 3 个级别，并在某一级成功后立即停止，不会无意义地继续升级。

### 诊断日志

可展开的日志面板，带时间戳，记录每一步操作，方便排查问题。

## 下载使用

对终端用户，推荐直接：

1. 从 GitHub Releases 下载最新 `.dmg`
2. 打开磁盘镜像
3. 把 `AirPods Fix.app` 拖到 `Applications`
4. 正常打开应用

## 运行要求

- macOS 13 (Ventura) 或更高版本
- AirPods（任何一代）或 AirPods Pro

## 从源码构建

源码构建和发布打包的细节见 [RUNTIME.md](RUNTIME.md)。

## 安装

构建完成后，把 app 移到你喜欢的位置：

```bash
# 用户应用目录
cp -r "AirPods Fix.app" ~/Applications/

# 或者系统应用目录（需要管理员权限）
sudo cp -r "AirPods Fix.app" /Applications/
```

也可以直接双击 `AirPods Fix.app` 运行，不用安装。

## 使用方法

1. 把 AirPods 连上 Mac
2. 打开应用，自动扫描已连接的 AirPods
3. 如果同时连了多副 AirPods，先在设备选择器里切到目标设备
4. 查看诊断面板，红色指示器代表有问题
5. 用**扬声器测试**确认音频输出
6. 用**麦克风测试**确认麦克风输入
7. 有问题就点**一键修复 AirPods**，应用会自动逐级尝试修复

### 症状对照表

| 症状 | 推荐修复 |
|---|---|
| 连上了但没声音 | 软修复（刷新音频路由） |
| 声音断断续续 | 中修复（重启 coreaudiod） |
| 一只耳机没声音 | 硬修复（蓝牙重连） |
| 卡在单声道/通话模式 | 中修复，然后检查是否有通话应用在运行 |
| 麦克风收不到声音 | 硬修复（重连会重建音频通道） |

## 工作原理

这是一个单文件 SwiftUI 应用（约 1800 行），使用：

- **CoreAudio API** 直接切换音频设备（比 AppleScript 更快更稳）
- **AVFoundation** 实时麦克风电平监测
- **system_profiler** 读取蓝牙设备信息和电量
- **blueutil** 程序化蓝牙断开/重连
- **osascript** 音量和静音控制

Unicode 归一化处理 macOS 智能引号（比如 AirPods Pro 设备名中的弯引号），避免匹配失败。

## 权限

应用会请求**麦克风权限**用于麦克风测试功能。这是可选的，其他所有功能不需要麦克风权限。

## 许可证

MIT
